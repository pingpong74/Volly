#pragma once

#include "core/device.hpp"

namespace Volly {
    struct RenderGraphCreateInfo {
        Volly::Device& device;
        Volly::Swapchain& swapchain;
    };

    class RenderGraph {

    };

    class RenderNode {

    };
}
